sudo: required
language: java
install: true
before_install: 
  - chmod +x mvnw
script: mvn package
services: 
  - docker
after_success:
  - mvn docker:build
  - test “$TRAVIS_BRANCH” = “master” && sh heroku.sh